{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {},
    "lint": {},
    "test": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
